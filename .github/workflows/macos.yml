name: go-macos

on:   
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    uses: cloudengio/.github/.github/workflows/go-test.yml@main
    with:
      go-versions: |
        [
          "1.23.x"
        ]
      os-version: macos-latest
