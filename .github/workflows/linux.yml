name: go-linux

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    uses: cloudengio/.github/.github/workflows/go-test-multimod.yml@locally-hosted-runners
    with:
      runner: |
        [ "ubuntu-latest" ]
      go-versions: |
        [ "1.25.x" ]
